@can($dataMethod)
<a href="{{ $routeName }}" class="btn btn-sm btn-outline-{{ $color }}" data-tooltip="tooltip" data-placement="top" title="{{ $title }}" target="{{ $target }}"><i class="fa fa-{{ $icon }}"></i></a>
@endcan